{% extends 'base.html.twig' %}

{% block title %}Affectation{% endblock %}

{% block body %}
    <h1>Affectation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ affectation.id }}</td>
            </tr>
            <tr>
                <th>Id_rub</th>
                <td>{{ affectation.idRub }}</td>
            </tr>
            <tr>
                <th>Id_org</th>
                <td>{{ affectation.idOrg }}</td>
            </tr>
            <tr>
                <th>Modif_auto</th>
                <td>{{ affectation.modifAuto ? affectation.modifAuto|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ affectation.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('affectation_index') }}">back to list</a>

    <a href="{{ path('affectation_edit', {'id': affectation.id}) }}">edit</a>

    {{ include('affectation/_delete_form.html.twig') }}
{% endblock %}
